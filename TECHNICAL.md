# Технічна Документація MVP

## Архітектура

### Загальний огляд

Застосунок побудований за принципом "Security by Design" з повним шифруванням на стороні клієнта. Жодні дані не покидають пристрій у незашифрованому вигляді.

```
┌─────────────────────────────────────────────────────┐
│                  User Interface                      │
│              (React Components)                      │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────┐
│              Application State                       │
│              (React Context)                         │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────┐
│            Business Logic Layer                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────┐  │
│  │ Vault Service│  │ TOTP Service │  │ Storage  │  │
│  └──────────────┘  └──────────────┘  └──────────┘  │
└──────────────────┬──────────────────────────────────┘
                   │
┌──────────────────▼──────────────────────────────────┐
│          Cryptography & Storage                      │
│  ┌──────────────┐  ┌──────────────┐                │
│  │  Web Crypto  │  │  IndexedDB   │                │
│  │     API      │  │              │                │
│  └──────────────┘  └──────────────┘                │
└─────────────────────────────────────────────────────┘
```

## Криптографія

### Генерація ключа

```typescript
// Параметри PBKDF2
const ITERATIONS = 100000;
const HASH = 'SHA-256';
const KEY_LENGTH = 256;

// Процес:
password → UTF-8 bytes → PBKDF2(salt, iterations) → AES-256 key
```

### Шифрування даних

```typescript
// Параметри AES-GCM
const ALGORITHM = 'AES-GCM';
const IV_LENGTH = 12 bytes;
const TAG_LENGTH = 128 bits;

// Процес:
plaintext → AES-GCM(key, iv) → ciphertext + auth tag
```

### Структура зашифрованого сховища

```json
{
  "salt": "base64-encoded-16-bytes",
  "iv": "base64-encoded-12-bytes",
  "data": "base64-encoded-encrypted-data",
  "version": 1
}
```

## Сховище даних

### IndexedDB Schema

```
Database: DecentralizedAuthDB
Version: 1

ObjectStore: vault
  - Key: "vault_data"
  - Value: {
      vault: EncryptedVault,
      lastAccess: number (timestamp)
    }
```

### Lifecycle даних

1. **Створення**:
   ```
   Password → Key → Encrypt([]) → Save to IndexedDB
   ```

2. **Розблокування**:
   ```
   Password → Key → Load from IndexedDB → Decrypt → Accounts[]
   ```

3. **Оновлення**:
   ```
   Modified Accounts[] → Encrypt with existing salt → Save
   ```

## TOTP Генерація

### Алгоритм

```
TOTP = HOTP(K, T)

де:
K = shared secret key
T = (Unix time - T0) / X
T0 = 0 (початок Unix часу)
X = 30 секунд (період оновлення)
```

### Параметри за замовчуванням

- **Algorithm**: SHA1 (сумісність)
- **Digits**: 6
- **Period**: 30 секунд

## Компоненти

### SetupScreen

**Призначення**: Onboarding, створення майстер-пароля

**Стан**:
- `password`: string
- `confirmPassword`: string
- `error`: string
- `loading`: boolean

**Валідація**:
- Мінімум 8 символів
- Паролі співпадають
- Strength score >= 3

### LockScreen

**Призначення**: Автентифікація для розблокування

**Стан**:
- `password`: string
- `error`: string
- `loading`: boolean

**Поведінка**:
- Auto-focus на input
- Очищення пароля при помилці

### MainScreen

**Призначення**: Головний інтерфейс

**Функції**:
- Відображення списку акаунтів
- Додавання нових акаунтів
- Видалення акаунтів
- Доступ до налаштувань
- Блокування

### AccountCard

**Призначення**: Відображення TOTP-коду

**Стан**:
- `code`: string (оновлюється кожну секунду)
- `timeRemaining`: number
- `progress`: number (0-1)
- `copied`: boolean

**Поведінка**:
- Auto-update кожну секунду
- Copy to clipboard при кліку
- Візуальний таймер

### AddAccountModal

**Призначення**: Додавання нового акаунту

**Режими**:
1. **QR Scanner**:
   - Використовує html5-qrcode
   - Парсить otpauth:// URI
   - Auto-fill форми

2. **Manual Entry**:
   - Issuer (обов'язково)
   - Username (опційно)
   - Secret key (обов'язково)

**Валідація**:
- Secret key - Base32 формат
- Issuer - не порожній

### SettingsModal

**Призначення**: Налаштування та бекап

**Функції**:
1. **Експорт**:
   - Генерація JSON
   - Download файлу

2. **Імпорт**:
   - Валідація JSON
   - Перевірка пароля
   - Заміна даних

## Безпека

### Потенційні вразливості та захист

1. **XSS Атаки**:
   - ✅ React автоматично екранує вміст
   - ✅ Немає dangerouslySetInnerHTML

2. **CSRF**:
   - ✅ Немає серверних API
   - ✅ Все локально

3. **Витоки пам'яті**:
   - ✅ CryptoKey не експортується
   - ✅ Очищення стану при блокуванні

4. **Timing Attacks**:
   - ⚠️ Потенційна вразливість при перевірці пароля
   - 🔜 TODO: Constant-time порівняння

5. **Shoulder Surfing**:
   - ✅ Auto-lock через 5 хвилин
   - ✅ Приховування пароля за замовчуванням

### Best Practices

1. **Не логуйте секрети**:
   ```typescript
   // ❌ Погано
   console.log('Password:', password);
   
   // ✅ Добре
   console.log('Password validation: OK');
   ```

2. **Очищуйте сенсітивні дані**:
   ```typescript
   // При блокуванні
   setState({ accounts: [], masterKey: null });
   ```

3. **Валідуйте вхідні дані**:
   ```typescript
   if (!validateSecret(secret)) {
     throw new Error('Invalid secret');
   }
   ```

## Тестування

### Ручне тестування

1. **Onboarding Flow**:
   - [ ] Створення слабкого пароля (має показати помилку)
   - [ ] Неспівпадаючі паролі (має показати помилку)
   - [ ] Успішне створення сховища

2. **Add Account**:
   - [ ] QR код з Google Authenticator
   - [ ] Ручне введення з валідним секретом
   - [ ] Ручне введення з невалідним секретом

3. **TOTP Codes**:
   - [ ] Коди оновлюються кожні 30 секунд
   - [ ] Copy to clipboard працює
   - [ ] Таймер відображається правильно

4. **Backup/Restore**:
   - [ ] Експорт створює файл
   - [ ] Імпорт з правильним паролем
   - [ ] Імпорт з неправильним паролем (помилка)

5. **Security**:
   - [ ] Auto-lock після 5 хвилин
   - [ ] Дані зберігаються зашифрованими
   - [ ] IndexedDB містить тільки зашифровані дані

### Unit Tests (TODO)

```typescript
// crypto.test.ts
describe('Crypto Utils', () => {
  test('generateSalt returns 16 bytes', () => {
    const salt = generateSalt();
    expect(salt.length).toBe(16);
  });

  test('encryption/decryption roundtrip', async () => {
    const password = 'test-password';
    const salt = generateSalt();
    const key = await deriveMasterKey(password, salt);
    
    const original = 'secret data';
    const iv = generateIV();
    const encrypted = await encrypt(original, key, iv);
    const decrypted = await decrypt(encrypted, key, iv);
    
    expect(decrypted).toBe(original);
  });
});
```

## Performance

### Оптимізації

1. **Lazy Loading**:
   - QR scanner завантажується тільки при відкритті модального вікна

2. **Мемоізація**:
   - TOTP коди обчислюються тільки раз на секунду
   - React.memo для компонентів

3. **Debouncing**:
   - Password strength перевірка

### Метрики

- **Initial Load**: < 2s
- **Time to Interactive**: < 3s
- **Unlock Time**: < 500ms
- **Add Account**: < 200ms

## Deployment

### Production Build

```bash
npm run build
```

Створює оптимізовану збірку в папці `dist/`.

### Hosting

Рекомендовані платформи:
- **Vercel** (автоматичний HTTPS)
- **Netlify** (автоматичний HTTPS)
- **GitHub Pages** (потрібен custom domain для HTTPS)

### Важливо

1. **HTTPS обов'язковий**:
   - Web Crypto API працює тільки через HTTPS
   - Service Workers потребують HTTPS

2. **Headers**:
   ```
   Content-Security-Policy: default-src 'self'
   X-Frame-Options: DENY
   X-Content-Type-Options: nosniff
   ```

## Troubleshooting

### Camera не працює

**Причина**: Відсутні дозволи або не HTTPS

**Рішення**:
1. Перевірте HTTPS
2. Дозвольте доступ до камери
3. Спробуйте інший браузер

### IndexedDB помилки

**Причина**: Private/Incognito режим

**Рішення**:
- Використовуйте звичайний режим браузера

### Service Worker не реєструється

**Причина**: Не HTTPS або dev-режим

**Рішення**:
- Зробіть production build
- Deploy на HTTPS домен

## Roadmap

### v1.1 (Post-MVP)

- [ ] Пошук по акаунтах
- [ ] Сортування
- [ ] Редагування акаунтів
- [ ] Біометрія (WebAuthn)

### v2.0

- [ ] Соціальне відновлення
- [ ] P2P синхронізація
- [ ] Темна тема
- [ ] Мультимовність

### v3.0

- [ ] Browser extension
- [ ] Mobile apps (React Native)
- [ ] Hardware keys support
